default:
    formatters:
        pretty:
            verbose:  true
            paths:    false
            snippets: false

    suites:
        default:
            contexts:
                - Context\FeatureContext

    extensions:
        Behat\MinkExtension:
            base_url:  'http://127.0.0.1:8000'
            sessions:
                default:
                    goutte: ~